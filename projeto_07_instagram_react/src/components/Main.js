import Stories from "./Stories";
import Posts from "./Posts";

export default function MainContent() {
    return (
        <main>
            <Stories />
            <Posts />
        </main>
    );
}